Hier ein paar Ideen was mir schon beim scouten aufgefallen ist:

- Generelle Funktionsweise:
 MoveModulesEverywhere lädt alle .xdl-Dateien, 
 macht aus jeder ein edu.byu.ece.rapidsmith.design.Module,
 für jedes dieser Module generiert es valide PLacements (von PrimitiveSites, ergo LUTs/CLBs/whatever aus gesehen),
 jedes dieser placements wird in ein Design-Objekt zurück erzeugt,
 jedes dieser Designs wird mit IO-Blocks versehen bzw die werden angepasst,
 dann wird das design als .xdl gedumpt (= unsere output-daten).

- dann wird mit dem ISE-Tool xdl2ndc das compiled,

- wenns dann einen fail produziert ging das umplacen nicht auf im sinne von:
  die logischen elemente konnte geplaced werden
